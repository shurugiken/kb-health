# Knowledge-Base Health Check

_Generated by kb_health_check.py over 4 notes in `examples/vault`. Review, then
fold the fixes back into your notes._

> This is real output from running `python kb_health_check.py examples/vault`
> against the bundled demo vault, so you can see what a report looks like
> without running the tool yourself.

## Contradictions

> WARNING: `setup.md` states Pathfinder requires **Python 3.11 or newer**; `index.md` states it supports **Python 3.8 and newer**. These are mutually exclusive minimum version requirements. One note must be corrected.

---

## Unsourced Claims

- `setup.md`: "Pathfinder is the fastest note indexer available." — superlative performance claim with no benchmark, test, or comparative source cited.

---

## Orphans

- `widget-scratch.md` — explicitly has no inbound or outbound links. No other note references it.

---

## Missing Pages

- **Querying** — `architecture.md` mentions an inverted index and queries staying fast, but no note explains how to run a query.
- **Configuration** — `index.md` links `setup.md` as "install and configure," but `setup.md` only covers installation; configuration is never documented.
- **Cache layer internals** — `architecture.md` mentions the cache layer three times and calls it the main reason for query speed, but no note documents its behavior, limits, or tuning options.

---

## Stale or Superseded

- `index.md`'s Python 3.8 minimum is likely the outdated claim, given that `setup.md` (a more specific install guide) asserts 3.11. If the project dropped 3.8–3.10 support at some point, `index.md` was never updated.

---

## Suggested New Links

- `architecture.md` → `setup.md`: `architecture.md` discusses the cache layer and index behavior; a reader who wants to configure or tune these would naturally need `setup.md`.
- `setup.md` → `architecture.md`: users who finish installation have no guided path to understanding how the tool works.

---

## New-Note Candidates

1. **`querying.md`** — How to run queries, query syntax, and what the inverted index supports.
2. **`configuration.md`** — All config options, file locations, and defaults.
3. **`cache.md`** — Cache layer behavior: what is stored, memory limits, invalidation rules.
4. **`changelog.md`** — Would anchor the Python version history and prevent future contradictions.

---

## Biggest Knowledge Gaps

1. **Usage** — No note shows how to actually use Pathfinder after installation.
2. **Configuration** — Promised in `index.md`'s link label but entirely absent.
3. **Performance characteristics** — A speed claim and a mechanism, but no concrete data.
4. **Python version support policy** — The contradiction signals no documented support policy.

---

## Writing-Style Flags

- `setup.md`: *"Pathfinder is the fastest note indexer available."* — violates Rule 1 (puffery/superlative). No benchmark is cited.
- `architecture.md`: *"The cache layer is the main reason queries stay fast."* — borderline Rule 2 (significance inflation). Acceptable only if followed by concrete data.

---

## TL;DR

Fix the Python version contradiction between `index.md` (3.8) and `setup.md` (3.11); add or link a configuration note to fulfill the promise made in `index.md`; remove or source the unsupported performance superlative in `setup.md`.
